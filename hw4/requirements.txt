# Core MCP + speech
mcp==1.16.0
kokoro==0.9.4
faster-whisper==1.2.0
soundfile>=0.12
numpy>=1.24

# spaCy runtime (you'll download the model separately)
spacy==3.8.*

# Agent + data model
crewai>=0.60,<1.0
pydantic>=2
